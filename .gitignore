# Build artifacts
dist/

# Node
node_modules/
npm-debug.log*

# Python
__pycache__/
*.pyc
.env

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Playwright — artefactos por corrida (scripts/test.sh sufija con el puerto
# para que dos corridas simultáneas no se pisen el informe)
playwright-report*/
test-results*/
test-results*.json

# fuentes/ — PDF, Excel y afiches originales del festival. Material de TRABAJO,
# no del producto: pesa decenas de MB y su sitio es el disco de Juan, no el repo.
# (Entraron 68 MB por un `git add -A` cuando esta regla aún no existía.)
fuentes/

# Claude Code (config local de harness, no del proyecto)
.claude/
