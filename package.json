{
  "name": "otrofestiv",
  "version": "1.0.0",
  "description": "Tu agenda para festivales de cine",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — no build step'",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
